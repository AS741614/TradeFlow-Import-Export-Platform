{
  "auto-typecheck-on-edit": {
    "PostToolUse": [
      {
        "matcher": "replace_file_content|multi_replace_file_content|write_to_file",
        "hooks": [
          {
            "type": "command",
            "command": "cd $(pwd) && npx tsc --noEmit 2>&1 | grep -E '\\.ts\\(' | head -5 || echo 'TSC clean'",
            "timeout": 30
          }
        ]
      }
    ]
  },
  "verification-reminder": {
    "Stop": [
      {
        "type": "command",
        "command": "echo 'Verification gate reminder: npx tsc --noEmit && npm run lint && npm test (x3) && npm run build before commit'"
      }
    ]
  }
}
