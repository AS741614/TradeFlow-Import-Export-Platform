/**
 * scripts/seed-default-org.ts
 * 
 * DEPRECATED: This script is now a no-op. The application dynamically provisions 
 * users and organizations through the signup page, and API routes are secured via 
 * session authentication. Static default IDs are no longer supported.
 */

console.log('scripts/seed-default-org.ts is DEPRECATED and now a no-op.');
process.exit(0);
